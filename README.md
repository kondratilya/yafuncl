Примеры кода example.txt

Todo
0. разобраться с return if. См. syntax.cpp, 235. То же, что и 2? просмотреть все jump на предмент контекстов где окажемся?
1. map - reduce - filter - for. Возможно отказаться от if - while, чтобы было пофункциональнее?
2. Внутри if else, return if, while все выражения, даже в "{}" выполняются через простой jump, jz, jnz, без создания контекста. Из-за этого не работает return true/false как continue/break в условиях.  Надо все подумать возможно и переделать чтобы функции сами создавали свои контексты, а не создавать их на переходе.
3. хочется сделать обращение к контекстам по имени переменной (типа объектов в JS и словарей в Python)
4. оператор ".переменная" который возвращает кортеж с ascii кодами букс переменной. Во-первых какая-то рефлексия, во вторых можно будет интереснее имитировать литералы
5. Ошибки Синтаксера (и лексера), а то очень неудобно. Сделать Follow(...)